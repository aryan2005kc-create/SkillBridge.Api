
services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    platform: linux/amd64  
    container_name: skillbridge-sql
    environment:
      SA_PASSWORD: "SkillBridge2026!"
      ACCEPT_EULA: "Y"
      mssql_PID: "Developer"
    ports:
      - "1433:1433"
    volumes:
      - sql-data:/var/opt/mssql
    restart: unless-stopped

volumes:
  sql-data: